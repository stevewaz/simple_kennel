org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Pinned to AGP 8.x (see android/settings.gradle.kts) instead of AGP 9's
# built-in-Kotlin model — cloud_firestore still self-applies the classic
# org.jetbrains.kotlin.android plugin and isn't AGP9-aware yet, which
# conflicts with builtInKotlin=true even though file_picker 11.x expects it.
# Revisit once every plugin in use has migrated.
android.newDsl=false
android.builtInKotlin=false
